# bytes_tools

Small byte-buffer helpers built on `std.bytes`.

This package is intentionally simple: it gives backend-oriented packages a shared place to start before AX grows native binary protocol, hashing, base64, TLS, and database wire helpers.

## Modules

- `bytes_tools.core`

