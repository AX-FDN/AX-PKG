# collection_tools

Small helpers for integer slices.

## Modules

- `collection_tools.ints`

## Example

```ax
import collection_tools.ints;

fn main() -> i32 {
    let values: [i32] = [1, 2, 3];
    return collection_tools.ints.sum(values);
}
```
