# encoding_tools

Pure AX encoding helpers for backend-oriented packages.

This preview package focuses on deterministic byte/string transforms that can run in the interpreter today. It is not a crypto package.

## Modules

- `encoding_tools.core`

