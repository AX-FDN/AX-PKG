# jwt_tools

JWT shape and unsigned-token helpers for AX package experiments.

This package does not sign or verify tokens. It is a safe staging package for header/payload shaping until AX has audited crypto/runtime primitives.

## Modules

- `jwt_tools.preview`

