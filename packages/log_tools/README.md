# log_tools

Small structured log formatting helpers for AX backend-style projects.

This preview package does not depend on clock/runtime APIs. Callers pass stable
service names and messages, and the helpers format deterministic log lines.
