# url_tools

Small URL classification and construction helpers for AX backend-style projects.

This preview package intentionally avoids full URL parsing. It provides stable
helpers that are useful before HTTP/TLS runtime support is complete.
