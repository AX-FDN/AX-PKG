# auth_tools

Small authentication-header helpers for AX backend-style projects.

This preview package does not implement cryptography, hashing, JWT signing, or
Base64. It provides safe string helpers that packages can use while native crypto
and byte-buffer support are still future work.
