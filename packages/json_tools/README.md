# json_tools

Small JSON string construction helpers for AX backend-style projects.

This preview package focuses on deterministic JSON emission for logs, API
payloads, smoke outputs, and package examples. It does not implement a full JSON
parser yet.
